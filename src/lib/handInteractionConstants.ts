export const ASSEMBLY_ZOOM_MIN = 0.25
export const ASSEMBLY_ZOOM_MAX = 5

/** Minimum palm separation when starting a two-fist zoom gesture. */
export const TWO_FIST_MIN_START_DIST = 0.06

/** Initial session reference distance before the first two-fist frame. */
export const TWO_FIST_DEFAULT_REF_DIST = 0.12
